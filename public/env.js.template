// Example runtime environment file.
// Copy or generate to `public/env.js` for runtime overrides (no rebuild needed when changed).
// Example content:
window._env_ = {
  REACT_APP_API_ENDPOINT: "http://18.234.170.77:3000/analyze_marksheet"
};
